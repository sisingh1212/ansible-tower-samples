- name: Hello World Sample
  hosts: all
  vars:

  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World!"
    - name: Print custom details
      debug:
        msg: "{{ item }}"
        loop:
          - '{{ slack_channel  }}'
          - '{{ slack_token }}'

