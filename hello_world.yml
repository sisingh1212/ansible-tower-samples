- name: Hello World Sample
  hosts: localhost
  #vars:
    #slack_channel: '{{ channel  }}'
    #slack_token: '{{ token }}'

  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World!"
    - name: Print custom details
      debug:
        msg: "{{ item }}"
      loop:
        - 1
        - 2
        #- '{{ slack_channel  }}'
        #- '{{ slack_token }}'

